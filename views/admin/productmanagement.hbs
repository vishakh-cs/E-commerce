<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link href="/dist/output.css" rel="stylesheet">
</head>
<body>
    <div class="p-6">
        <h2 class="text-3xl font-semibold mb-6">Product Management</h2>
        
        <!-- Product List/Table -->
        <div class="overflow-x-auto">
            <div class="w-full overflow-x-scroll">
                <table class="w-full divide-y divide-gray-200 border rounded-lg">
                    <thead>
                        <tr>
                            <!-- Product Image Header -->
                            <th class="px-6 py-10 bg-gray-100 text-left text-xs leading-4 font-semibold text-gray-600 uppercase tracking-wider">
                                Product Image
                            </th>
                            
                            <!-- Product Name Header -->
                            <th class="px-6 py-3 bg-gray-100 text-left text-xs leading-4 font-semibold text-gray-600 uppercase tracking-wider">
                                Product Name
                            </th>
                            
                            <!-- Price Header -->
                            <th class="px-6 py-3 bg-gray-100 text-left text-xs leading-4 font-semibold text-gray-600 uppercase tracking-wider">
                                Price
                            </th>
                            
                            <!-- Quantity Header -->
                            <th class="px-6 py-3 bg-gray-100 text-left text-xs leading-4 font-semibold text-gray-600 uppercase tracking-wider">
                                Quantity
                            </th>
                            
                            <!-- Description Header -->
                            <th class="px-6 py-3 bg-gray-100 text-left text-xs leading-4 font-semibold text-gray-600 uppercase tracking-wider">
                                Description
                            </th>
                            
                            <!-- Category Header -->
                            <th class="px-6 py-3 bg-gray-100 text-left text-xs leading-4 font-semibold text-gray-600 uppercase tracking-wider">
                                Category
                            </th>
                            
                            <!-- Rating Header -->
                            <th class="px-6 py-3 bg-gray-100 text-left text-xs leading-4 font-semibold text-gray-600 uppercase tracking-wider">
                                Rating
                            </th>
                            
                            <!-- Offers Header -->
                            <th class="px-6 py-3 bg-gray-100 text-left text-xs leading-4 font-semibold text-gray-600 uppercase tracking-wider">
                                Offers
                            </th>
                            
                            <!-- Actions Header -->
                            <th class="px-6 py-3 bg-gray-100 text-left text-xs leading-4 font-semibold text-gray-600 uppercase tracking-wider">
                                Actions
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Loop through your product data and populate the table rows -->
                        {{#each products}}
                        <tr>
                            <td class="px-6 py-4 whitespace-no-wrap">
                                <!-- Loop through the images array and display each image -->
                                {{#each this.images}}
                                <div class="w-16 h-16 bg-gray-200 rounded-lg overflow-hidden">
                                    <!-- Update the src attribute to use the current image filename -->
                                    <img src="/uploads/{{this}}" alt="{{../name}}" class="w-full h-full object-cover">
                                </div>
                                {{/each}}
                            </td>
                            <td class="px-6 py-4 whitespace-no-wrap">{{this.name}}</td>
                            <td class="px-6 py-4 whitespace-no-wrap text-lg font-semibold text-green-600">${{this.price}}</td>
                            <td class="px-6 py-4 whitespace-no-wrap">{{this.quantity}}</td>
                            <td class="px-6 py-4 whitespace-no-wrap text-blue-900">{{this.description}}</td>
                            <td class="px-6 py-4 whitespace-no-wrap text-blue-900">{{this.category}}</td>
                            <td class="px-6 py-4 whitespace-no-wrap">{{this.rating}}</td>
                            <td class="px-6 py-4 whitespace-no-wrap text-blue-900">{{this.offers}}</td>
                            <td class="px-6 py-4 whitespace-no-wrap space-x-2">
<a href="/edit/{{this._id}}" class="text-indigo-600 hover:text-indigo-900 font-semibold">Edit</a>
                                <form method="post" action="/delete/{{this._id}}">
        <button type="submit" class="text-red-600 hover:text-red-900 font-semibold">Delete</button>
    </form>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <a href="/admindashboard">dashboard</a>
</body>
</html>
