<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Success</title>
    <!-- Include Tailwind CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.7/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 h-screen flex items-center justify-center">
    <div class="container mx-auto p-8 bg-white rounded-lg shadow-lg">
        <h1 class="text-3xl font-semibold mb-8 text-center text-green-600">Order Successfully Placed!</h1>
        <div class="user-details mb-6">
            <h2 class="text-xl font-semibold mb-2">User Details</h2>
            {{!-- <p><strong>Name:</strong> {{user.name}}</p> --}}
            <p><strong>Email:</strong> {{user.email}}</p>
            <!-- ... (other user details) -->
        </div>
        <div class="product-details">
            <h2 class="text-xl font-semibold mb-2">Product Details</h2>
            <div class="flex items-center mb-4">
                <div class="w-24 h-24 mr-4">
                    <img src="/uploads/{{product.images}}" alt="{{product.name}}" class="w-full h-full rounded-lg object-cover">
                </div>
                <div>
                    <p><strong>Name:</strong> {{product.name}}</p>
                    <p><strong>Price:</strong> ${{product.price}}</p>
                    <!-- ... (other product details) -->
                </div>
            </div>
        </div>
        <div class="mt-8 text-center">
            <a href="/" class="bg-blue-500 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-full inline-block transition duration-300 ease-in-out">Continue Shopping</a>
        </div>
    </div>
</body>
</html>
